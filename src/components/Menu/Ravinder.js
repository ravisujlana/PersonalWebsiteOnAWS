import React from 'react';

import { Link } from "react-router-dom";

const Ravinder = props => {

    return (
        <div className="header header1">
            <Link to="/" className="dropdown-item">Ravinder</Link>
        </div>
    )
};

export default Ravinder;
